'use strict';

const DEFAULT = Object.freeze({
  CONTEXT_LOG_PROPERTY_NAME: 'context_log',
  LEVELS: Object.freeze(['error', 'warn', 'info', 'verbose', 'debug', 'silly'])
});


module.exports = Object.freeze({
  DEFAULT
});
